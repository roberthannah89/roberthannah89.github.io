// Copy this file to local-config.js and fill in your real keys.
// local-config.js is git-ignored so the keys never get committed.
//
// See docs/google-maps-embed-setup.md for how to get a Maps Embed API key.
window.HIKING_CONFIG = {
  // Google Maps Embed API key. Used by the transit widget's "Compare with
  // Google Maps" pane. Leave blank to show setup instructions instead.
  googleMapsApiKey: "",
};
